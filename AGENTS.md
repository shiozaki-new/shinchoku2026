# 大阪展示会 思鳴会議室 開発ガイド（Codex用）

## 最初に読む

作業前に必ず [`Docs/思鳴の継.md`](Docs/思鳴の継.md) を読む。
作業の合否はこの文書の「許」で決める。

次に以下を実行して現在地を取得する。

```sh
sh Scripts/session_start.sh
```

未コミット差分、リモートからの遅れ、機械検査の不成立があれば、作業より先に報告する。
`git pull` は自動実行しない。

## 正本の順序

参照は上から下への一方向とする。

1. [`Docs/認書.md`](Docs/認書.md) ─ 解く課題
2. [`Docs/思書.md`](Docs/思書.md) ─ 未確定の素材。実装根拠にしない
3. [`Docs/鳴書.md`](Docs/鳴書.md) ─ 確定した戦略と許
4. [`Docs/示書.md`](Docs/示書.md) ─ 実装方針
5. [`Docs/会議項目.json`](Docs/会議項目.json) ─ 会議項目の機械可読な現在地
6. コード ─ 具体値と現物

経緯と再発防止は [`Docs/継の記録.md`](Docs/継の記録.md) に主題別で置く。
日記は書かない。

## 固定規則

- 大阪展示会固有の目的、来場者、勝ち筋、成功条件をAIが推測で確定しない。
- 新しい展示物や機能へ降りる前に「なぜ作るのか」を一度だけオーナーへ聞く。
- 会議項目には成立判定、上限、定め手、次の操作を必ず持たせる。
- 未確定の項目は `認` または `思` に置く。確定していない値を `鳴` 以降へ置かない。
- 思鳴との矛盾を見つけたら、矛盾する一点だけを止める。黙って実装せず、黙って縮めない。
- チャットの合意はその場で消える。確定事項は同じ作業中にGitHub上の正本へ反映する。
- 鍵、個人情報、未公開の契約情報、会場の保安情報をリポジトリへ書かない。
- HTMLへ値を手で二重転記しない。会議項目は `Docs/会議項目.json` から表示する。

## 鳴分岐

思から鳴へは次の三経路だけを使う。

- 成立：判定可能な文になったため、確定して示へ進む
- 思戻り：中央で確定できる新しい素材が出たため、上限内で再試行する
- 打切り：新しい素材が無い、または上限へ到達したため、前提化か探索型を選ぶ

成立判定または上限が無い場合は実装へ降りない。

## 変更後の判定

```sh
python3 Scripts/handover_guard.py
```

この検査が通らない変更はコミットしない。検査を外す迂回経路を追加しない。

更新日：2026-08-26
