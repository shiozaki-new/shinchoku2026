# Header/Hero 実装計画（designsheet/0001.png 準拠）

作成日: 2026-01-13
対象: `src/components/01_Header.jsx`, `src/components/02_Hero.jsx`
参照: `designsheet/0001.png`, `DESIGN.md`

---

## 基本方針（DESIGN.md 準拠）
- コンテンツ幅: 1200px（アートボード 1920px / コンテンツ幅 1200px）。
- フォント: Noto Sans JP 系（DESIGN.md 記載の3種いずれか）。
- ベースカラー: `#333333`, `#004f80`, `#4098d4`, `#036eb7`, `#2cb9e7`, `#e6d226`, `#ffffff`。
- 行送り: 本文はフォントサイズの200%（例: 16px → 行送り32px）。

---

## 01_Header 仕様
- コンテナ
  - `max-width: 1200px` を中心配置。左右パディングは 24px（`px-6`）を想定。
  - ヘッダー自体は `sticky top-0 z-50`。
- 高さ
  - 行高（バー高）目安: 72px（現行値）。PNGでの明示なしのため維持。
- ロゴ
  - 高さ: 45px（PNG注記: H:45px）。現行実装: `h-[45px] w-[203px]`。
- ナビゲーション
  - 文字: 18px／色 `#333333`（PNG: 18px #333333）。
  - リンク間隔: 48px 目安（現行 `space-x-12`）。
  - 装飾三角: CSS 三角（`border-l:7px; border-r:7px; border-top:12px; color:#036eb7;`）。現行実装と一致。
- 右側CTAボタン
  - 共通: 角丸フル、テキスト色 `#333333`、高さ 45px、幅 190px（PNG: W:190px H:45px）。
  - 「資料ダウンロード」: 背景 `#e6d226`。ホバーは明度微調整。
  - 「お問い合わせ」: 白背景＋2px相当のグラデ枠（`from #4098d4` → `to #036eb7`）。
- モバイル
  - ハンバーガーアイコン 24px、開閉で縦積み。CTAは幅100%相当。

---

## 02_Hero 仕様
- セクション寸法/レイアウト
  - 最小高: `min-height` 660px（LG想定）。
  - 背景: 左側はグラデーション（`#2cb9e7 → #036eb7 → #004f80`）。右側は背景画像（`/images/hero_right_bg.webp`）。
  - 左背景のデザイン幅: 960px（PNG: W:960px）。背景としてvw基準で再現。コンテンツは1200pxコンテナ内に収める。
  - コンテナ: `max-width: 1200px`, 水平パディング 24px。
  - グリッド: LGで 12カラム（左7/右5）を基本。
- テキストブロック（左）
  - アイブロー「営業組織向け」: 20px, 太め, `#ffffff`（PNG: 20px）。
  - サブコピー「営業マネージャーの…連携」: 20px, 太め, `#ffffff`（PNG: 20px）。
  - H1「dbSheet for SFA」: 80px, 極太, `#ffffff`（PNG: 80px）。`®`は上付き、サイズ 0.4em。
  - アンダーライン: 幅 70px, 高さ 5px, `#ffffff`（PNG: W:70px H:5px）。
  - 本文: 16px, 行送り32px（200%）。色はPNG注記 `#333333`。背景が濃色のため実装は `white/90` を提案（要確認）。
  - CTA: 「資料ダウンロード」「お問い合わせ」共に W:190px H:45px（ヘッダーと同仕様）。
- 右ブロック（見出し/画像）
  - サブ見出し「大手企業を中心に 600 社の導入」: 40px, 太字, 色 `#004f80`（PNG: 40px, #004f80）。
  - 見出し左右の装飾（ローレル）: 各 `W:17px H:50px`（PNG注記）。
  - メイン画像（ラップトップ）: 幅 700px 目安（PNG: W:700px）。
  - 下部ロゴ2点: 高さ 45px, 間隔 50px（PNG: H:45px, gap約50px）。

---

## Tailwind 実装ノート（クラス指針）
- コンテンツ幅: `max-w-[1200px] mx-auto px-6`。
- H1（LG以上）: `text-[80px] leading-[1.1] font-extrabold`。
- アイブロー/サブコピー: `text-[20px] font-semibold text-white`。
- 本文: `text-[16px] leading-[32px] text-white/90`（PNGは#333333指定。背景とのコントラスト要確認）。
- サブ見出し: `text-[40px] font-extrabold text-[#004f80]`。
- アンダーライン: `w-[70px] h-[5px] bg-white rounded-full`。
- CTA: `w-[190px] h-[45px] rounded-full text-[14px] font-bold`。

---

## 現行との差分（要対応）
- `src/components/01_Header.jsx`
  - [x] ロゴ H:45px → 現行一致。
  - [ ] コンテナ最大幅を `max-w-[1200px]` に調整（現行: `max-w-[1700px]`）。
  - [ ] ナビ文字サイズを 18px に固定（現行: 14px）。
  - [x] CTAサイズ/色は概ね一致（念のため `w-190px h-45px` を明示）。
- `src/components/02_Hero.jsx`
  - [ ] コンテナ最大幅を `max-w-[1200px]` に調整（現行: `max-w-[1400px]`）。
  - [ ] アイブロー/サブコピーを 20px に（現行: 16px）。
  - [ ] H1 を 80px（現行: 32/42/55px）に。`sup` 0.4em 維持。
  - [ ] アンダーラインを `w-[70px] h-[5px]` に（現行: `w-28 h-[6px]`）。
  - [ ] 本文を 16px/32px に（現行: 14px + 独自leading）。色は要確認。
  - [ ] サブ見出しを 40px & 色 `#004f80`（現行: 22/26/28px & `#036eb7`）。
  - [ ] ラップトップ画像の最大幅を 700px に（現行: 640px）。

---

## 検証手順
- ビューポート 1920px で確認し、コンテンツ幅が 1200px に収まっていること。
- 主要文字サイズ（18px/20px/40px/80px）が DOM/CSS で一致すること。
- CTA の実寸（190×45）が一致し、ホバー時のコントラストが十分であること。
- 背景グラデーションの色が `#2cb9e7 → #036eb7 → #004f80` になっていること。
- アクセシビリティ: 色コントラスト（WCAG AA相当）を満たすか確認。

---

## 未確定・要確認事項
- 本文色: PNGでは `#333333` 指定だが、ヒーロー背景が濃色のため `#ffffff/90` を提案。どちらで統一しますか？
- ヘッダーバー高: 現行 72px。PNGに明示なし。変更希望はありますか？
- サブ見出し左右の装飾（ローレル）の正確な画像/サイズ: 現在の `hero_heading_left/right.svg` で問題ないか。

---

## 実装タスク（チェックリスト）
- [ ] Header: コンテナ幅調整 + ナビ文字サイズ 18px 化。
- [ ] Hero: コンテナ幅 1200px 化。
- [ ] Hero: アイブロー/サブコピー 20px 化。
- [ ] Hero: H1 80px 化 + アンダーライン調整。
- [ ] Hero: 本文 16px/32px 化（色確定後に反映）。
- [ ] Hero: サブ見出し 40px & `#004f80` 化。
- [ ] Hero: 画像幅 700px に調整。
- [ ] レスポンシブ確認（LG未満のフォールバックの最適化）。
